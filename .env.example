# AbacatePay secret API key (https://abacatepay.com → Integrar → Chaves de API).
# Use a dev-mode key locally and set the production key on Vercel
# (Project → Settings → Environment Variables). Never commit real keys.
ABACATEPAY_API_KEY='teste'
